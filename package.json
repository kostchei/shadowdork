{
  "name": "shadowdork",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "eslint src tests --max-warnings 365 && knip --include exports --max-issues 62 --max-show-issues 10 && knip --include types --max-issues 32 --max-show-issues 10 && knip --exclude exports,types"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "knip": "^6.27.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.65.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
